<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="96dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="4dp"
    android:gravity="center_vertical"
    android:background="@color/white"
    android:paddingLeft="18dp"
    android:paddingRight="18dp">

    <ImageView
        android:id="@+id/iv_group_dp"
        android:layout_width="64dp"
        android:layout_height="64dp"
        tools:src="@tools:sample/avatars" />

    <ImageView
        android:id="@+id/iv_lock_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignBottom="@id/iv_group_dp"
        android:layout_alignRight="@id/iv_group_dp"
        android:background="@drawable/circle_white"
        android:padding="2dp"
        android:visibility="gone"
        tools:visibility="visible"
        android:tint="@color/dark_gray"
        app:srcCompat="@drawable/ic_lock_black_24dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/iv_group_dp"
        android:layout_alignBottom="@id/iv_group_dp"
        android:layout_marginLeft="8dp"
        android:layout_toRightOf="@id/iv_group_dp"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <androidx.emoji.widget.EmojiTextView
            android:id="@+id/tv_group_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/black"
            android:textSize="16sp"
            tools:text="Group Name sdfiu hsifuhs dfhsud fhsudfh usisi" />

        <androidx.emoji.widget.EmojiTextView
            android:id="@+id/tv_group_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            tools:text="Group Descriptiion asgdasu " />
    </LinearLayout>

</RelativeLayout>